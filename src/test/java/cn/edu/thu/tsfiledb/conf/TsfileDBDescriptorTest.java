package cn.edu.thu.tsfiledb.conf;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class TsfileDBDescriptorTest {

    @Before
    public void setUp() throws Exception {
    }

    @After
    public void tearDown() throws Exception {
    }

    @Test
    public void test() {
//	TsfileDBConfig config = TsfileDBDescriptor.getInstance().getConfig();
//	assertEquals(config.rpcPort, 6666);
//	assertEquals(config.enableWal, false);
//	assertEquals(config.walFolder, "wal");
//	assertEquals(config.walCleanupThreshold, 100000);
//	assertEquals(config.flushWalThreshold, 20000);
//	assertEquals(config.flushWalPeriodInMs, 100);
//	assertEquals(config.overflowDataDir, "data/overflow2");
//	assertEquals(config.fileNodeDir, "data/digest2");
//	assertEquals(config.bufferWriteDir, "data/delta2");
//	assertEquals(config.metadataDir, "metadata2");
//	assertEquals(config.derbyHome, "derby2");
//	assertEquals(config.mergeConcurrentThreads, 0);
//	assertEquals(config.maxOpenFolder, 10);
//	assertEquals(config.fetchSize, 100);
	
    }

}
