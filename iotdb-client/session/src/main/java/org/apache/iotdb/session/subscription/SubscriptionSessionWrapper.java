package org.apache.iotdb.session.subscription;

final class SubscriptionSessionWrapper extends SubscriptionSession {

  public SubscriptionSessionWrapper(String host, int port) {
    super(host, port);
  }
}
